# Paper grid team — hourly evidence review

Owner: **Grid Desk Lead** in native Grok Bot. Destination: **Paper Grid Trading Team**.
Trigger: **single Codex heartbeat, every hour at :15 Europe/Bucharest**, after
the minute-05 radar. Native timer is PAUSED; its retained instructions are this
playbook. Accept the uniquely identified external controller cue as authorized
hourly work. See [current controller](controller.md); never run competing timers.
This is a reproducible description; editing this file does not edit the native routine.

Use the v2 standing contract and assigned existing role mandates across both
rooms. Use public autopilot/radar plus sanitized packets from the existing
read-only operations steward. Compare source timestamps and IDs with the last completed
review. A new fill count alone is not a new independent trading outcome.

First recover the board, reconcile overdue needs, check due daily research and
inspect radar core/bench candidate changes separately from open positions. Process
that due work without a manual prompt. Only then consider the quiet branch.

If there is no due work, meaningful new close, material risk/data change, actionable evidence
or required operator action, stay quiet and do not ping specialists. If material,
request one note from each needed core role: Data & Structure, Technical
Interpreter, Risk Sentinel, Performance Analyst and Strategy Manager. Use
Paper Grid Research & Data for X Setup Researcher, Research Scout and existing
operations/data steward, with native bot mentions and attributed handoffs. Choose one
snapshot generation as the review ID and disclose different source times.

Each specialist replies once. Grid Desk Lead then synthesizes actual findings once:
evidence time; data health; positions and net outcome; strategy/risk exceptions;
one supported hypothesis; and canonical advisory HOLD / REVIEW / ESCALATE.
HOLD means no supported change, REVIEW means a testable unresolved issue, ESCALATE
means a material risk/data failure. Record engine effect separately as NO_CHANGE
unless an actual engine event is independently evidenced.
Absent specialists stay pending. Contradictory risk/data findings cannot be outvoted.
Insufficient samples remain HOLD; unchanged holds do not require another alert.

Preserve 12–200 grids, 5x, strictly >1% estimated pair return on allocated margin
after both 0.06% fill fees, confirmed chart bounds, entry BUY/SELL targets LONG40/60,
SHORT60/40, NEUTRAL50/50 with one-order tolerance, core max5 and direction max4.
The radar stale cutoff is >120 minutes, not 15 minutes. Expected admission activity
is >=2 grids/hour; current realized activity may differ. Existing positions may
survive demotion. Never infer entry splits from today's ladder.

Prefer published net. Engine bot net = realized_pnl + unrealized_pnl - fees_paid -
funding_paid. Never add gross grid_profit again; negative funding_paid is a credit.
Reconcile retained closed outcomes and state truncation. Research sample suggestions
never replace the existing learning gates. No orders, funds, source/state edits,
new private account/computer/SSH access, additional bots or additional native schedules.
The existing steward may write sanitized board and isolated experiment outputs
under the two approved team sandbox directories; never engine/rule state.

Report freshness at source time, not guaranteed live health. Say no breach
observed in available fields; leave unverified entry splits or live safeguards
unknown. Distinguish bounded history from demonstrated truncation.

A manual group review, a native server signal and a completed external scheduled
invocation are separate acceptance facts.
Do not claim the first external scheduled invocation succeeded until the
controller receipt and attributed group/board result confirm it. Routine modifications use the existing
routine, not a duplicate.

_Last verified: 2026-09-13_


## Version 2 daily research and board

The same routine performs one daily09:15 Europe/Bucharest research pass, or first
later run when that date has not been completed. X Setup Researcher returns one
original-X hypothesis or an honest access block; Research Scout returns one
primary-source paper/documentation/YouTube check. Technical Interpreter maps
compatible evidence to our strategy. Performance/Risk assess it and Manager
records its lifecycle stage. The orchestrator reconciles both rooms' task board
and closes or blocks each task. No duplicate daily run or separate schedule.

Maintain task_id, owner, actual reported state, source_asof, strategy_version,
updated_at, evidence, dependencies and blocker in native conversation/memory.
Read the last completed board before dispatch; UNKNOWN is not DONE. Retry a
missing handoff once then BLOCKED. The native Manager register is not connected
as an approval control to the existing07:00 applier; novel rules remain shadow.


Read the Supabase evidence file through the existing steward, retaining historical
source times and missing coverage. The same controller
refreshes it in the daily08:15 phase; it is not another trade/review routine. Read and acknowledge sandbox bridge results;
unknown rules require implementation and all replay results remain shadow. Track
new operational needs automatically with owner, measurable acceptance and next
review trigger. Continue unaffected tasks while one source is blocked.


For a due supported hypothesis, Manager supplies the specification; existing
steward uses the operator-installed bridge's context and exact request protocol.
Run at most one new evaluation per hourly round and three per local date, dedupe
by review date/rule/evidence as well as request ID. Relay generated result.json and
metrics to Manager, Performance and Risk, then record the final stage and result
reference. Never copy generated sandbox proposals into the live writer input.
A changed pinned revision is a maintenance need, not automatic repin authority.


Read changed engineering-status.json through the existing steward and reconcile
its need ID, tests, commit/PR and blocker into the shared board. The Codex controller's
10:15 phase processes at most one scoped engineering need; it does not
edit this board, deploy live changes, repin the installed bridge or add a writer.
A PR is READY_FOR_REVIEW, not an applied upgrade. The08:15 data phase and10:15
engineering phase share the hourly Codex heartbeat and maintain phase deduplication.


After every material round or changed need/result, require one atomic board write
and readback from the steward before durable completion. Record the returned
board version/time. One failed retry becomes BOARD_PERSIST_BLOCKED; preserve
actual chat findings without claiming restart-safe completion. Do not repeat the
completed evaluation. Store actual receipt strategy_revision, evidence_hashes and
request_sha256. The logical duplicate key is review_date + rule_id +
strategy_revision + evidence_hashes, with request ID retained as artifact reference.
